# Base URL of the AwaBus API. In local dev, Vite proxies /api and /socket.io
# to VITE_API_PROXY_TARGET (see vite.config.js) so this can stay unset.
# Set it for a production build pointing at a deployed API.
VITE_API_URL=
