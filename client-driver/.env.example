# Base URL of the AwaBus API, reachable from the driver's phone (not
# "localhost" — use your machine's LAN IP when testing with Expo Go, e.g.
# http://192.168.1.20:5000/api).
EXPO_PUBLIC_API_URL=http://192.168.1.20:5000/api
